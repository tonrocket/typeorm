"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
require("reflect-metadata");
const test_utils_1 = require("../../utils/test-utils");
const UserEntity_1 = require("./entity/UserEntity");
const chai_1 = require("chai");
describe("github issues > #5365 Generated Identity for Postgres 10+", () => {
    let connections;
    before(async () => (connections = await (0, test_utils_1.createTestingConnections)({
        entities: [UserEntity_1.User],
        schemaCreate: false,
        dropSchema: true,
        enabledDrivers: ["postgres"],
    })));
    after(() => (0, test_utils_1.closeTestingConnections)(connections));
    it("should produce proper SQL for creating a table with identity column", () => Promise.all(connections.map(async (connection) => {
        const sqlInMemory = await connection.driver
            .createSchemaBuilder()
            .log();
        (0, chai_1.expect)(sqlInMemory)
            .to.have.property("upQueries")
            .that.is.an("array")
            .and.has.length(1);
        (0, chai_1.expect)(sqlInMemory.upQueries[0])
            .to.have.property("query")
            .that.eql(`CREATE TABLE "user" ("id" integer GENERATED BY DEFAULT AS IDENTITY NOT NULL, CONSTRAINT "PK_cace4a159ff9f2512dd42373760" PRIMARY KEY ("id"))`);
    })));
});
//# sourceMappingURL=issue-5365.js.map